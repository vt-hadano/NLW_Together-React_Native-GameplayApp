import { 
    REDIRECT_URI,
    SCOPE,
    RESPONES_TYPE,
    CLIENT_ID,
    CDN_IMAGE
} from "./discordAuth";

export { 
    REDIRECT_URI,
    SCOPE,
    RESPONES_TYPE,
    CLIENT_ID,
    CDN_IMAGE
}